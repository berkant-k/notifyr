## What this changes

<!-- A sentence or two. Link the issue if there is one. -->

## Why

<!-- The problem being solved. If the code looks odd without a constraint,
     say what the constraint is — that reasoning belongs in a comment too. -->

## Checklist

- [ ] `npm run lint` passes
- [ ] `npm run typecheck` passes
- [ ] `npm test` passes
- [ ] `npm run build` passes
- [ ] Tests added or updated for behaviour changes
- [ ] Documentation updated if behaviour, the API, or a limitation changed
